export const preventNavWarning = (event) => {
  console.log(event);
  // event.preventDefault();
  // const confirmationMessage = '변경사항이 저장되지 않을 수 있습니adasdsads다';

  // event.returnValue = confirmationMessage;

  // return confirmationMessage;
};
